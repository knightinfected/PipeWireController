Source: pipewire-control-center
Section: sound
Priority: optional
Maintainer: knightinfected <hmzmahmood5@gmail.com>
Build-Depends: debhelper-compat (= 13)
Standards-Version: 4.7.0
Homepage: https://github.com/knightinfected/PipeWireController
Rules-Requires-Root: no

Package: pipewire-control-center
Architecture: all
Depends: ${misc:Depends},
         python3 (>= 3.10),
         python3-gi,
         python3-gi-cairo,
         gir1.2-gtk-4.0,
         gir1.2-adw-1 (>= 1.4),
         python3-numpy,
         python3-soundfile,
         pipewire (>= 1.0),
         pipewire-bin,
         wireplumber (>= 0.5)
Recommends: pipewire-pulse,
            pulseaudio-utils
Suggests: lsp-plugins-lv2,
          carla
Conflicts: pipewire-controller
Replaces: pipewire-controller
Provides: pipewire-controller
Description: GTK4/libadwaita control center for PipeWire
 A graphical control center for PipeWire that exposes everything under
 PipeWire's Configuration documentation as toggles and dropdowns, so none of it
 has to be reached by hand-editing .conf files.
 .
 Signal Paths builds the route audio actually takes — sources with their own
 effect chain sending into mixes with their own chain and outputs. Alongside it
 are a parametric equalizer with live A/B compare, microphone cleanup, live
 level meters on every volume control, a drag-to-connect patchbay, performance
 monitoring, virtual devices, LADSPA/LV2 effect inserts, per-application
 policies, routing snapshots and HRIR virtual surround.
 .
 All persistent changes are written as drop-in configuration files, never to
 the base files, so every setting can be reverted cleanly.
